let count=0;const cart=document.querySelector('#cart b'),toast=document.querySelector('#toast'),form=document.querySelector('#form'),menuToggle=document.querySelector('.menu-toggle'),nav=document.querySelector('#siteNav');const digits='۰۱۲۳۴۵۶۷۸۹';function showToast(message){if(!toast)return;toast.textContent=message;toast.classList.add('show');window.setTimeout(()=>toast.classList.remove('show'),1800)}document.querySelectorAll('.add').forEach(button=>button.addEventListener('click',()=>{count++;if(cart)cart.textContent=count<10?digits[count]:String(count);showToast(`${button.dataset.name} به سبد اضافه شد.`)}));form?.addEventListener('submit',event=>{event.preventDefault();if(!form.reportValidity())return;showToast('پیام شما ثبت شد.');form.reset()});menuToggle?.addEventListener('click',()=>{const isOpen=nav?.classList.toggle('open')??false;menuToggle.setAttribute('aria-expanded',String(isOpen))});nav?.querySelectorAll('a').forEach(link=>link.addEventListener('click',()=>{nav.classList.remove('open');menuToggle?.setAttribute('aria-expanded','false')}));